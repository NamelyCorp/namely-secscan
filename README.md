# Namely SecScan

Namely SecScan is a read-only VPS security scanner that generates a full audit report.

## Usage
```bash
chmod +x namely-secscan.sh
sudo ./namely-secscan.sh
```

Reports are saved under `/root/security-reports/`.

## License
MIT
